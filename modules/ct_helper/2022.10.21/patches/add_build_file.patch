--- /dev/null
+++ BUILD.bazel
@@ -0,0 +1,13 @@
+load("@rules_erlang//:erlang_app.bzl", "erlang_app")
+
+erlang_app(
+    app_name = "ct_helper",
+    erlc_opts = select({
+        "@rules_erlang//:debug_build": ["+debug_info"],
+        "//conditions:default": [
+            "+deterministic",
+            "+debug_info",
+        ],
+    }),
+    stamp = 0,
+)
