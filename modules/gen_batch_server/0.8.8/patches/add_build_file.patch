--- /dev/null
+++ BUILD.bazel
@@ -0,0 +1,68 @@
+load("@rules_erlang//:untar.bzl", "untar")
+load("@rules_erlang//:app_file.bzl", "app_file")
+load("@rules_erlang//:erlang_bytecode.bzl", "erlang_bytecode")
+load("@rules_erlang//:erlang_app_info.bzl", "erlang_app_info")
+
+untar(
+    name = "contents",
+    outs = [
+        "LICENSE",
+        "LICENSE-APACHE2",
+        "LICENSE-MPL-RabbitMQ",
+        "README.md",
+        "rebar.config",
+        "rebar.lock",
+        "src/gen_batch_server.app.src",
+        "src/gen_batch_server.erl",
+    ],
+    archive = "contents.tar.gz",
+)
+
+erlang_bytecode(
+    name = "beam_files",
+    srcs = ["src/gen_batch_server.erl"],
+    hdrs = [],
+    dest = "ebin",
+    erlc_opts = select({
+        "@rules_erlang//:debug_build": ["+debug_info"],
+        "//conditions:default": [
+            "+deterministic",
+            "+debug_info",
+        ],
+    }),
+)
+
+app_file(
+    name = "app_file",
+    app_description = "Generic batching server",
+    app_name = "gen_batch_server",
+    app_src = ["src/gen_batch_server.app.src"],
+    app_version = "0.8.8",
+    dest = "ebin",
+    modules = [":beam_files"],
+    stamp = 0,
+)
+
+erlang_app_info(
+    name = "gen_batch_server",
+    srcs = [
+        "src/gen_batch_server.app.src",
+        "src/gen_batch_server.erl",
+    ],
+    hdrs = [],
+    app = ":app_file",
+    app_name = "gen_batch_server",
+    beam = [":beam_files"],
+    license_files = [
+        "LICENSE",
+        "LICENSE-APACHE2",
+        "LICENSE-MPL-RabbitMQ",
+    ],
+    visibility = ["//visibility:public"],
+)
+
+alias(
+    name = "erlang_app",
+    actual = ":gen_batch_server",
+    visibility = ["//visibility:public"],
+)
