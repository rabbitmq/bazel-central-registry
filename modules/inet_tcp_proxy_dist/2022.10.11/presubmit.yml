matrix:
  platform:
  - centos7
  - debian10
  - ubuntu2004
  - macos
  - windows
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    build_targets:
    - '@inet_tcp_proxy_dist//:erlang_app'
    - '@inet_tcp_proxy_dist//:inet_tcp_proxy_dist'
